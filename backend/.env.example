PORT=4000
# Dev DB connection (docker-compose: host=db внутри контейнера API)
DATABASE_URL=postgres://postgres:postgres@db:5432/ijshub

# Optional: test DB connection (used only when NODE_ENV=test)
# TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/ijshub_test
JWT_SECRET=supersecret_jwt_key_change_me
